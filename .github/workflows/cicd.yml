name: todoList 自动部署
on:
  push:
    branches:
      - main
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20.x
      - run: npm install -g pnpm
      - run: pnpm install
      - run: pnpm run build
      - name: action-Deploy
        uses: peaceiris/actions-gh-pages@v4
        with:
          deploy_key: ${{ secrets.UTILITY_ADMIN_SECRET }}
          publish_branch: gh-pages
          publish_dir: ./dist

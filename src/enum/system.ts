// 主题
export enum Theme {
  LIGHT = 'light',
  DARK = 'dark',
  AUTO = 'auto',
}

// 系统布局
export enum Layout {
  TOP,
  LEFT,
  MIX,
}

// 系统语言
export enum Locale {
  ZH = 'zh',
  EN = 'en',
}

export enum TabActionKeys {
  RELOAD,
  MAXMIZE,
  NEW_WINDOW_OPENS,
  CLOSE_CURRENT,
  CLOSE_LEFT,
  CLOSE_RIGHT,
  CLOSE_OTHER,
  CLOSE_ALL,
}

export enum AnimationTypes {
  FADE = 'fade',
  FADE_SLIDE = 'fade-slide',
  FADE_BOTTOM = 'fade-bottom',
  FADE_SCALE = 'fade-scale',
  ZOOM_FADE = 'zoom-fade',
  ZOOM_OUT = 'zoom-out',
}

export enum TabBarStyle {
  CARD,
  GOOGLE,
}
